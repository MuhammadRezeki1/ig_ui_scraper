# ── FastAPI Backend ──────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3

# ── Scraper Engine (dari project lama kamu) ──────
playwright==1.49.0
requests==2.32.3
python-dotenv==1.0.1
colorama==0.4.6
emoji==2.14.0
langdetect==1.0.9

# ── Sentiment Analysis (IndoBERT) ────────────────
# Uncomment kalau pakai mode hybrid / ml_only
# transformers==4.46.3
# torch==2.5.1

# ── Gemini (opsional, validator) ─────────────────
# google-generativeai==0.8.3

# ── Flask (kalau masih mau pakai CLI lama) ───────
# flask==3.1.0
# flask-cors==5.0.0
